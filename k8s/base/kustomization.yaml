apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: crypto-platform

namespace: crypto-platform

# Common labels applied to all resources
commonLabels:
  app.kubernetes.io/name: crypto-platform
  app.kubernetes.io/version: "1.0.0"
  app.kubernetes.io/managed-by: kustomize

# Resources to include
resources:
  - namespace.yaml
  - configmap.yaml
  - secrets.yaml
  - persistent-volumes.yaml
  - postgres.yaml
  - redis.yaml
  - backend.yaml
  - worker.yaml
  - frontend.yaml
  - ingress.yaml

# Images - Update these for your registry (ghcr.io, dockerhub, etc.)
# For local development with minikube, use local image names
images:
  - name: crypto_platform-backend
    newTag: latest
  - name: crypto_platform-frontend
    newTag: latest
  - name: crypto_platform-worker
    newTag: latest
